import 'package:flutter/material.dart';

class LayoutView extends StatelessWidget {
  static const String id = 'LayoutView';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('LayoutView'),),
    );
  }
}
